import { experimental_sx as sx } from "@mui/material"

export const MuiSelect = {
    "styleOverrides": {
        root: sx({
            ".MuiOutlinedInput-root": {
                borderColor: "border",
                borderRadius: 0.3,
            },

        })
    }
}